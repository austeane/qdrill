// import { handle as authHandle } from '$lib/server/auth';

// export const handle = async ({ event, resolve }) => {
// 	// Delegate handling authentication to authHandle
// 	return authHandle({ event, resolve });
// };